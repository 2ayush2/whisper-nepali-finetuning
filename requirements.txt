# Core dependencies (Windows compatible)
torch>=2.0.0
transformers>=4.40.0
datasets>=3.4.1
accelerate
peft
python-dotenv
evaluate
jiwer
huggingface_hub>=0.34.0
sentencepiece
protobuf
librosa
soundfile

# Note for Windows users:
# triton, unsloth, and torchcodec are Linux-only
# For Windows local training, use WSL2 or run in Google Colab
